workflows:
  android-workflow:
    name: Android Build
    environment:
      flutter: stable
    scripts:
      - echo "=== المسار الحالي ==="
      - pwd
      - echo "=== محتويات المجلد ==="
      - cmd /c dir
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk